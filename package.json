{
  "name": "moki-docus",
  "version": "0.0.1",
  "description": "Moki Docus",
  "scripts": {
    "start": "npm run dev",
    "dev": "dumi dev",
    "build": "dumi build",
    "prepare": "husky install && dumi setup"
  },
  "authors": [
    "ClearLuvMoki 2893096286@qq.com"
  ],
  "license": "MIT",
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "lint-staged": {
    "*.{md,json}": [
      "prettier --write --no-error-on-unmatched-pattern"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^17.6.6",
    "@commitlint/config-conventional": "^17.6.6",
    "dumi": "^2.2.1",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "moki-editor": "^0.2.1",
    "prettier": "^2.8.8"
  }
}
